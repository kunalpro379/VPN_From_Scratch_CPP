#include "../Include/Authentication.hpp"
#include "../Utils.hpp"
bool Authentication::authenticateClient(const std::string& username, const std::string& password){
Utils::logMessage("Authenticating client with username: " + username + " and password: " + password);


}
bool Authentication::authenticateClient(int clientSocket){

}
